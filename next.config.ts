import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,
  allowedDevOrigins: ['superresponsibly-preponderant-micki.ngrok-free.dev'],
};

export default nextConfig;
